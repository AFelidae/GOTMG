class ShootBullet{
  constructor(forward,right,properties){
    this.forward = forward //Relative to monster shooting them as origin, always facing the player
    this.right = right
    this.properties = properties
  }
}
